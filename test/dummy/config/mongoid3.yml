development:
  sessions:
    default:
      database: canard_development
      hosts:
        - localhost:27017

test:
  sessions:
    default:
      database: canard_test
      hosts:
        - localhost:27017
      options:
        connect_timeout: 0.01
        wait_queue_timeout: 0.01
        socket_timeout: 0.01
